import { mapState, mapActions } from "vuex";

export default {
    name: "FindPasswordMixin",
    computed: {
        ...mapState({

        })
    },
    methods: {
        ...mapActions({

        })
    }
}
