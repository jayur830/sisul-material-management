import { mapState, mapActions } from "vuex";

export default {
    name: "FindUsernameMixin",
    computed: {
        ...mapState({

        })
    },
    methods: {
        ...mapActions({

        })
    }
}
