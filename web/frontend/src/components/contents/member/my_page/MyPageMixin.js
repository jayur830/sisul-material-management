import { mapState, mapActions } from "vuex";

export default {
    name: "MyPageMixin",
    computed: {
        ...mapState({

        })
    },
    methods: {
        ...mapActions({

        })
    }
}
