import { mapState, mapActions } from "vuex";

export default {
    name: "SignUpMixin",
    computed: {
        ...mapState({

        })
    },
    methods: {
        ...mapActions({

        })
    }
}
