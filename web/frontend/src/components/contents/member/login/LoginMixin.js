import { mapState, mapActions } from "vuex";

export default {
    name: "LoginMixin",
    computed: {
        ...mapState({

        })
    },
    methods: {
        ...mapActions({

        })
    }
}
