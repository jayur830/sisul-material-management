import { mapState, mapActions } from "vuex";

export default {
    name: "MaterialMixin",
    computed: {
        ...mapState({

        })
    },
    methods: {
        ...mapActions({

        })
    }
}
